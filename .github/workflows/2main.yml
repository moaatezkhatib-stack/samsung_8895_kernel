- name: Fix permissions
  run: |
    chmod -R +x toolchain/
    chmod -R +x scripts/

- name: Create config
  run: |
    make ARCH=arm64 exynos8895-dreamlte_defconfig

- name: Build
  run: |
    export ANDROID_MAJOR_VERSION=9
    make ARCH=arm64 -j4
